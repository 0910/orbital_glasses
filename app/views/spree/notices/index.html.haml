.row-fluid
  - @notices.each do |notice|
    .span3
      = link_to(notice, class: 'box smallbox') do
        - unless notice.images.empty?
          .ph
            = image_tag(notice.images.first.attachment.url(:large))
          .over
            %h1= notice.title
            %p ver detalle
